---
- include: ansible-version.yml

#- hosts: monitored
#  become: false
#  remote_user: root
#  gather_facts: true
#  roles:
#    - role: adm
#      adm_passwd:
#        - "{{ passwd.art }}"
#        - "{{ passwd.darkk }}"

- hosts: monitored
  gather_facts: true
  vars:
    letsencrypt_nginx: yes
  roles:
    - node_exporter
    - node_exporter_nginx
