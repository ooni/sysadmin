# Generated by ansible
# roles/dehydrated/templates/letsencrypt-http

server {
    # Listen on port 80 for *any* domain
    listen 80;
    server_name _;

    # Serve ACME challenge from disk
    location ^~ /.well-known/acme-challenge {
        alias /var/lib/dehydrated/acme-challenges;
    }
}
